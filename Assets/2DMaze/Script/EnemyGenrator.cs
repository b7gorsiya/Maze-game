using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyGenrator : MonoBehaviour
{
    /*
    public void Instansiate_enemy()
    {
        var clmn = Random.Range(0, columns / 2);
        var row = Random.Range(0, rows / 2);
        Instantiate(enemyPrefeb, cells[row, clmn].floor.transform.position, Quaternion.identity);
        FindObjectOfType<EnemyAI>().currentColumn = clmn;
        FindObjectOfType<EnemyAI>().currentRow = row;
    }
    */
}
